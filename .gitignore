# Dependencies
node_modules/
.pnpm-store/

# Environment — NEVER committed, no exceptions and no template.
# Env files are `.env` + `.env.local` per side, local only. There is
# deliberately no `.env.example`: the setup is not shared with everyone who
# can read this repo. Do not re-add a whitelist here.
.env
.env.*

# Build output
dist/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# Test / coverage
coverage/
playwright-report/
test-results/
playwright/.auth/

# Prisma — generated artifacts
backend/prisma/*.db

# Sentry
.sentryclirc

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Private working knowledge — never commit
.claude-workbook/

# Internal working docs - never committed
docs/
archive/
MASTER_TODO.md
PLUG_AND_PLAY.md
SETUP_TODO.md
