# Error Log

| Date | Project | Sev | Symptom | Root cause | Fix | Prevention | Related |
|------|---------|-----|---------|------------|-----|------------|---------|
| 2026-09-24 | Admin Dashboard | P1 | `pnpm audit` reported 2 critical, 22 high, 16 moderate, and 1 low dependency vulnerabilities across the workspace. | Direct packages and the shared lockfile still resolved vulnerable Next.js, Multer, Nodemailer, Vitest, and transitive dependency releases; the Nightly audit surfaced the findings but deliberately did not mutate the dependency graph. | Upgraded the affected direct packages, added narrowly versioned workspace overrides for vulnerable transitives, regenerated the lockfile, and validated Prisma generation, both applications' static checks/builds, and all backend/frontend tests. The remediated graph reports 0 critical, 0 high, 1 moderate, and 0 low findings. | Keep the Nightly audit warning actionable, review every transitive override when its parent updates, and require `pnpm audit`, frozen-lockfile installation, full tests, and production builds before merging dependency-remediation PRs. | `.github/workflows/nightly.yml`; `pnpm-workspace.yaml`; GHSA findings from the 2026-09-24 audit |
| 2026-09-24 | Admin Dashboard | P2 | The remediated dependency graph still reports one moderate `uuid` advisory (`GHSA-w5hq-g745-h8pq`) through `backend > exceljs > uuid@8.3.2`. | ExcelJS 4.4.0 pins the unsupported `uuid` 8 line while the advisory is patched only in `uuid >=11.1.1`; overriding it would cross three major versions. The affected v3/v5/v6 buffer-writing APIs are not called by this application, so the reachable risk is lower than the compatibility risk of an unowned major override. | Accepted this single finding temporarily rather than forcing an unsafe override; all critical/high findings remain removed and the exception is explicit in the committed error log. | Recheck on every ExcelJS release and remove the exception when ExcelJS adopts `uuid >=11.1.1`; until then, do not add direct `uuid` v3/v5/v6 calls or pass caller-owned buffers to those APIs. | `exceljs@4.4.0`; `uuid@8.3.2`; `GHSA-w5hq-g745-h8pq`; dependency-remediation entry above |
| 2026-09-23 | Admin Dashboard | P2 | A successful Nightly audit run displayed vulnerable dependencies in its log but skipped the dedicated warning step. | The audit used `continue-on-error`, which turns its job-level conclusion successful; the following `if: failure()` therefore evaluated false even though the audit step's raw outcome was failure. | Gave the audit step an id and conditioned the warning on `steps.dependency_audit.outcome == 'failure'`. | For intentionally continued checks, branch on the named step's `outcome`; reserve `failure()` for non-continued failures that set the job failure state. | `.github/workflows/nightly.yml`; run `35906951608` |
| 2026-09-23 | Admin Dashboard | P1 | Scheduled Nightly run `35832442674` failed during pnpm setup before dependency installation or audit could run. | The workflow specified pnpm `11` in `pnpm/action-setup` while root `package.json` also specified `pnpm@11.5.0`; action-setup v6 rejects two version authorities because the major-only value can resolve to a different patch. | Removed the workflow-level version and kept `package.json#packageManager` as the single source of truth. | Never pass `version` to `pnpm/action-setup` when `packageManager` exists; search every workflow when changing the repository package-manager pin. | `.github/workflows/nightly.yml`; run `35832442674` |
| 2026-09-23 | Admin Dashboard | P1 | Full backend test runs could fail later integration cases with unexpected HTTP `429` responses when the developer's backend `.env` set `NODE_ENV=development`. | `vitest.setup.ts` loaded `.env` and then used `process.env.NODE_ENV ??= 'test'`; the existing development value survived, so application modules enabled runtime rate limiters during tests and made results dependent on suite order. | Changed the setup to unconditionally set `NODE_ENV=test` before application imports and added a regression test that starts from an inherited development environment. | Test harnesses must hard-set execution-mode variables that alter import-time behavior; regression-test the setup from a conflicting inherited environment rather than relying on CI's clean environment. | `backend/vitest.setup.ts`; `vitest-setup.test.ts`; storefront checkout rate-limit note |
| 2026-09-23 | Admin Dashboard | P1 | PR #258 backend CI passed 1,495 tests but `product-content.test.ts` failed when the Arabic public catalogue response had no iterable `data`. | The branch fixture predated the public catalogue's fail-closed `isSellingPoint` requirement, so the API correctly returned `404`; the test skipped response-status assertions and surfaced a misleading TypeError instead. | Marked the catalogue branch fixture as a selling point and asserted every parallel API response is `200` before inspecting its body. | Public storefront fixtures must opt into `isSellingPoint`; when testing parallel requests, assert every response status before dereferencing any response body so the real contract failure remains visible. | `backend/src/__tests__/product-content.test.ts`; PR #258 |
| 2026-09-23 | Admin Dashboard | P1 | A branch-selected day timeline could include staff audit events from another branch even though its order rows were correctly scoped. | The timeline merged `Order` and `AuditLog` but applied `branchWhere(params)` only to the order query; its audit half was written against the older schema assumption that audit rows had no branch column, then rebased after branch-aware audit landed. | Applied the selected `branchId` to the audit query, selected and resolved audit branch labels with order branch labels, and added a two-branch integration test that seeds foreign order and audit events and asserts both are excluded. | Every merged report must test its scope predicate independently for every source table. When a prerequisite schema branch lands, re-audit comments and fallback assumptions during rebase instead of resolving only textual conflicts. | `getDayTimeline`; `day-timeline.test.ts`; branch reporting PR #255 |
| 2026-09-21 | Admin Dashboard | P1 | PR #255 backend CI reported one failed suite with all 1,479 collected tests passing; `product-content.test.ts` failed to transform because `businessId` and `branchId` were declared twice. | Two commits in the stacked branch independently added the same branch-fixture declarations; each change was reasonable in isolation, but their combined tip contained duplicate lexical declarations, and the backend build/typecheck excludes test files. | Removed the redundant declaration pair and verified the focused suite plus the complete backend suite on the combined PR tip. | After rebasing or stacking fixture changes, inspect and test the combined tip rather than relying on validation of each commit; run the full backend Vitest command before pushing the updated stack. | `backend/src/__tests__/product-content.test.ts`; PR #255 |
| 2026-09-21 | Admin Dashboard | P1 | Branch-scoping PR caused broad backend integration failures: scoped reads returned `404`, reports omitted fixture data, and test factories hit invalid constraints. | Existing tests created global orders, stock movements, audit rows, and users without the explicit branch membership/context required by the fail-closed branch model; a few fixtures also relied on insertion order or exceeded schema limits. | Made branch ownership explicit in fixtures and request helpers, corrected invalid factory data, and aligned assertions with the scoped roster contract. | Require any new scoped integration fixture to declare `branchId` and use the shared scoped-auth helper; add a CI test that runs branch-scope fixtures against a fresh migrated database. | N/A |
| 2026-09-23 | Admin Dashboard | P1 | Retrying `POST /api/v1/public/orders` after a lost response could create a second order, redeem a discount twice, and decrement branch inventory twice. | Public checkout used one transaction per attempt but had no durable idempotency claim tying retries to the first committed result. | Required a UUID `Idempotency-Key`, executed the storefront checkout inside the shared idempotency transaction, replayed the stored result, rejected changed payloads with `409`, and exposed `Idempotency-Replayed`. | Every externally retryable money or inventory mutation must have replay, changed-request, and single-side-effect integration tests; keep rate limiting disabled only in the test environment so business-rule cases cannot become order-dependent. | `backend/src/routes/v1/public.route.ts`; `backend/src/services/storefront.service.ts`; `backend/src/__tests__/storefront-checkout.test.ts` |
