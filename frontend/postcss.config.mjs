// Tailwind CSS v4 — configuration is CSS-first (see src/app/globals.css).
// There is no tailwind.config.js in v4; theme tokens are declared with
// @theme inside the stylesheet.
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
